# セッション49(2026-06-18)— 測量士 P1 修正 + client_steward test 昇格 + 横展開2号(パナHM)

## 一行サマリ

測量士の手紙(6/17)を起点に **finance/client_steward の「静かな欠落・二重登録」を固め**(P1①②③ + 回帰テスト3本)、**client_steward を VPS デプロイ → test 昇格**(cron 月08:20 / bot 配線 / `--domain` フラグ)、その勢いで **横展開2号=パナソニックホームズを bootstrap して soil 台帳化**(メールのみで縦通し)。

---

## 1. 測量士の手紙(2026-06-17)への応答 + P1 修正

手紙はコード実読で裏取り → **行番号まで正確**と確認。応答欄に採用/保留を記入([letter](../surveyor/letters/2026-06-17.md))。

| | 指摘 | 対応 | 検証 |
|---|---|---|---|
| **P1①** | finance importer が部分失敗後の再実行で**二重記帳**し得る(全件成功時のみ input 退避という既知の穴の部分失敗ケース) | `state/registered_ledger.jsonl`(row hash = 日付\|金額\|摘要\|部門\|occurrence)で登録直後に追記 → 再実行で登録済み行を skip。失敗通知も「同じ CSV 再実行可」に変更 | 回帰テスト:run1=2登録/1失敗 → run2=2 skip/失敗分のみ登録 → run3=全 skip。**1000/3000 の post は各1回**(二重なし) |
| **P1②** | auditor の未登録明細検出が **1口座100件で打ち切り**(ページングなし → PL 過少検出) | `freee_client.get_all_wallet_txns()`(offset ループ + max_pages 安全弁)追加 → auditor を全ページ化 | 回帰テスト:250件→全件 / 満ページ100 / 不足30 / 空0 / max_pages 打ち切り |
| **P1③** | Freee refresh token 共有が reload だけでは**同時 refresh 競合**を防げない | refresh+save を `fcntl.flock` で排他、lock 取得後に disk 再読込(S39 reload は fast-path として残置・後方互換) | py_compile OK。競合窓は今の cron 時刻分散では実質開かないが安全弁として実装 |

- **freee_client は正本修正 → `sync-freee-client.sh` で4 service にコピー配布**(`--check` GREEN)。**finance は VPS 反映済**(importer/auditor/freee_client + test 2本、VPS でも GREEN)。
- ⚠️ **expense/invoice/shift の freee_client コピーは token-lock(P1③)のみで非緊急(後方互換)→ 各自の次回デプロイで反映**。

回帰テスト3本(`garden/services/finance/test/` 2本 + `garden/services/client-steward/test/` 1本)。すべて外部依存ゼロ(google/Freee を叩かない)、ローカル・VPS とも GREEN。

## 2. client_steward を test 昇格(draft → test)

測量士 P2④⑤ を test 昇格作業に含めて実装:
- **P2④ watermark 取りこぼし** = `list_thread_ids` を nextPageToken で全ページ化。上限到達時は `truncated=True` で **watermark を進めない**。`save_watermark` は now() でなく**処理した最新スレッド時刻**まで進める(同日後着の取りこぼし防止)。
- **P2⑤ シグナルが件名だけ** = finance/schedule 判定を **本文 snippet も**走査(metadata 取得でも snippet は付く=追加 API 不要)。

**VPS デプロイ**(client-steward は完全新規配置):
- service rsync + venv(--without-pip+get-pip)+ `.env`(**`SOIL_CLIENTS_DIR`=garden-mirror**。VPS の `/garden/soil` は空で実体は garden-mirror 配下 = invoice の `SOIL_STAFF_DIR` と同型。**デプロイに必須だった修正**)
- seed/SKILL rsync + **cron 登録(月 08:20)** + status `draft → test`
- **bot 配線**「クライアント見て」(`CLIENT_TOPICS` + client_block、sweep は read-only digest)+ settings.json に venv python 絶対パス `:*` allow + **bot 再起動**(新 PID 260016 online)
- **VPS dry-run GREEN**(MTI digest 正常・📅 シグナルが本文 snippet 由来で発火=P2⑤ 実証・担当者2名署名取得)

**残(active 昇格)= 種の初回自動発火 6/22(月)08:20 の digest を見届け**。

## 3. 横展開2号 — パナソニックホームズ(B)

「MTI 型をもう1社で磨く」(測量士推奨)を実走。

- **`--domain` フラグ実装**(B が突き当てた A の課題をその場で解消)= soil 足場なしで任意ドメインを digest。新会社をドメインだけで引くのが1コマンドに。
- **Bootstrap 引き**(panasonic-homes.com・過去6ヶ月・**71スレッド**)→ digest → **ガクチョ剪定**(案件1本「みずき台住民イベント運営」/ リクルート山口=契約関係なしのコーディネーター / 直近6ヶ月)
- **soil 台帳を MTI 型で起こした**:[企業正本](../../garden/soil/clients/panasonic-homes/README.md) / [案件台帳](../../garden/soil/clients/panasonic-homes/projects/みずき台住民イベント運営/README.md) / [メールレーン](../../garden/soil/clients/panasonic-homes/projects/みずき台住民イベント運営/emails/2025H2-2026_みずき台イベント.md) / 担当者5ページ
- **クロスリンク** = toB-pipeline ①②(120/110万)→ 台帳、projects/README に横展開2号反映
- **横展開の実証** = soil に足すだけで **VPS sweep が両社(MTI+パナHM)を自動巡回**

**MTI との違い** = 研修でなく**継続運営**(みずき台住民イベント・四半期請求)、**Plaud 会議録なし=メールだけで案件骨格が立った** → 「横展開の起点は常にドメイン、Plaud は上乗せ」を実証。

## 次セッションへの宿題

### client_steward
- **★ 初回 cron 発火 6/22(月)08:20 の見届け → test→active**
- **digest → soil 台帳ドラフト生成の自動化**(今回も Claude が手で起こした=「真の一発 bootstrap」の最後のピース。SKILL「soil 自動 append + board 起草」次段)
- 横展開3号以降(ゴンチャ / boundlesslife / 白井松 / 三井 / 京急)= MTI/パナHM 参照
- 次段(SKILL 既定):d/f 見積・請求の自動生成(PDF/Excel)/ c 資料生成 / e 実績レポート / Brief(打合せ前)/ Plaud cron 取り込み(MCP ブリッジ)/ sweep の儀礼メール誤検出 polish
- パナHM 担当者5名の個別役割の確定(本文確認 → board)

### finance
- **expense/invoice/shift の freee_client コピー**に P1③(token lock)を次回各デプロイで反映(非緊急・後方互換)
- **パナHM 請求(10-12月・今年度後半)の freee 反映確認**(bootstrap で顕在化。発行済だが記帳要確認)→ finance Mode A/D で突合
- 既存宿題:月次サイクル初回1周見届け / MTI 経営者研修(税抜約290万・2月入金確認済だが toB-pipeline 未掲載=FY 計上取りこぼし候補)/ 新人研修227万 freee 確認 / 4月 STORES・5月 MTI227万/白井松75万 の未反映

### 継続
- 測量士の次回手紙 / 他区画の初回発火見届け(sns 週次・field・shift finalize 7/8・invoice 7/12)
