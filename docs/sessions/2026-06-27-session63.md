# セッション63(2026-06-27) — SNS IG job15 復旧 + ig_scheduler publish retry

## 発端

Discord/番人から以下の通知が届いた。

- `IG 投稿失敗(ig_scheduler job 15 / ig_carousel)`
- 予定: `2026-06-26T23:00:00Z` = 2026-06-27 08:00 JST
- error: `400 Client Error: Bad Request for url: https://graph.facebook.com/v21.0/17841404542535531/media_publish`
- caption 冒頭: `三浦半島、野比の山でお茶を育てています。 京急電鉄さん所有の森。...`

ガクチョから「状況確認、できればそのまま即時投稿」と依頼。

## 確認したこと

S57 の教訓に従い、local repo ではなく VPS の実体を確認した。

- `ig_scheduler` コンテナ名は `ig_scheduler`
- 実DBはコンテナ内 `/data/scheduler.db`
- host 側 `/home/vps-harappa/ig_scheduler/scheduler.db` は空ファイルで、実DBではない
- job15 の保存画像URLは FB CDN ではなく Drive 公開URL
- IG直近投稿に該当投稿は出ておらず、重複投稿はなかった

job15 の実態:

- platform: `ig_carousel`
- status: `failed`
- image_url: Drive URL 5枚
- ログ上はカルーセル子アイテム5件作成成功
- 親カルーセルコンテナ `18409766032151418` 作成成功
- 最後の `media_publish` だけ 400

このため、今回の主因は通知文の定型文にある「FB CDN URL失効」ではなく、Meta側で親カルーセルコンテナ作成直後に publish するタイミングが早すぎた可能性が高い。

## 復旧

既存の親カルーセルコンテナを再 publish した。

- `creation_id`: `18409766032151418`
- publish 結果: `18105251096046066`
- 投稿URL: https://www.instagram.com/p/DaERebelB8-/
- IG直近投稿で `CAROUSEL_ALBUM` として掲載確認

その後、ig_scheduler DB の job15 を補正。

- status: `published`
- post_id: `18105251096046066`
- error_msg: `null`

## 再発防止

`ig_scheduler` に `media_publish` retry を追加した。

変更ファイル:

- [`vps/ig_scheduler/app.py`](../../vps/ig_scheduler/app.py)
- [`deploy/ig_scheduler/app.py`](../../deploy/ig_scheduler/app.py)

内容:

- `_publish_container()` を追加
- `media_publish` を最大6回、10秒間隔で retry
- Meta API のエラー本文を最大500文字まで例外に含める
- 単写真とカルーセルの両方で `_publish_container()` を使う
- 古い再デプロイ元だった `deploy/ig_scheduler/app.py` を現行カルーセル対応版へ同期し、将来のデプロイで単写真版へ巻き戻る事故を防止

## デプロイと復旧

`bash scripts/deploy_ig_scheduler.sh` でVPSへ反映。

途中、Docker Compose v1 の既知系エラー `KeyError: 'ContainerConfig'` で再作成が止まり、`ig_scheduler` が一時停止した。

対応:

1. `docker ps -a --filter name=ig_scheduler` で停止コンテナを確認
2. 停止コンテナを削除
3. `docker-compose up -d --no-build` でビルド済みimageから起動

復旧確認:

- `ig_scheduler` container: Up
- `http://localhost:8100/health`: OK
- job14/job15: `published`
- VPS上の `/home/vps-harappa/ig_scheduler/app.py` に retry 実装が反映済み

## 検証

- `python3 -m py_compile vps/ig_scheduler/app.py deploy/ig_scheduler/app.py`
- IG直近投稿で `18105251096046066` / https://www.instagram.com/p/DaERebelB8-/ を確認
- DB job15 が `published` / `error_msg=null`
- `ig_scheduler` health OK

## git / 注意

今回の作業で変更した tracked files:

- `garden/MAP.md`
- `docs/sessions/2026-06-27-session63.md`
- `vps/ig_scheduler/app.py`
- `deploy/ig_scheduler/app.py`

作業開始時点から存在していた未追跡ファイルは触っていない:

- `docs/surveyor/letters/2026-06-25-01.md`
- `garden/soil/clients/mti/projects/経営研修2026/meetings/2026-06-26_経営WS活動設計_社内打合せ.md`

## 次回への申し送り

- ig_scheduler の通知文は「画像URL失効(FB CDN)等」と定型で出しているが、今回のように Drive URL + publish timing の失敗もある。将来、番人通知は DB の `image_url` が `drive.google.com` か `fbcdn.net` かを見て原因候補を出し分けるとよい。
- `scripts/deploy_ig_scheduler.sh` は `docker-compose up -d --build` で Compose v1 の `ContainerConfig` 問題に当たることがある。再発時は停止コンテナ削除→`docker-compose up -d --no-build` で復旧できる。
