# garden-couchdb env (VPS のみに配置、git 管理外)
# 配置先: ~/garden/services/couchdb/.env (chmod 600)
# 生成: openssl rand -base64 32

COUCHDB_USER=garden-admin
COUCHDB_PASSWORD=__GENERATE_WITH_OPENSSL_RAND_BASE64_32__
